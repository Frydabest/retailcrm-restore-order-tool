# 🔄 Инструмент восстановления заказов CRM

PHP-утилита для восстановления удалённых заказов из CRM-системы через API. Инструмент анализирует историю заказов, реконструирует их полное состояние и создаёт новые заказы со всеми деталями.

## ✨ Возможности

- **Умная реконструкция истории** - Прослеживает всю историю заказа для восстановления полного состояния
- **Возобновление работы** - Автоматически продолжает с последнего обработанного заказа при прерывании
- **Отслеживание изменений товаров** - Обрабатывает добавление, удаление и изменение товаров
- **Управление адресами** - Сохраняет и восстанавливает изменения адреса доставки
- **Расчёт скидок** - Автоматически рассчитывает и применяет общую скидку на товары
- **Детальное логирование** - Подробные логи для мониторинга и отладки

## 📋 Требования

- PHP 7.4 или выше
- Composer
- Guzzle HTTP client
- Действующие учётные данные API CRM

## 🚀 Установка

1. Клонируйте репозиторий

2. Установите зависимости:
```bash
composer require guzzlehttp/guzzle
```

3. Настройте учётные данные в `orders_restore.php`:
```php
define("URL", "https://your-crm-url.ru");
define("KEY", "ваш-api-ключ");
```

## 📝 Использование

1. Подготовьте файл `orders.csv` со списком ID заказов (по одному на строку):
```
212
213
214
215
```

2. Запустите скрипт:
```bash
php orders_restore.php
```

3. Следите за процессом в консоли и логе `restore.log`

## 🔧 Как это работает

1. **Чтение истории** - Скрипт получает полную историю каждого заказа через API
2. **Реконструкция состояния** - Отслеживает все изменения товаров, адресов и других полей
3. **Поиск удалённого заказа** - Находит запись с флагом `deleted` в истории
4. **Создание заказа** - Восстанавливает заказ с полным состоянием на момент удаления
5. **Сохранение прогресса** - Записывает ID обработанного заказа для возможности возобновления

## 📂 Структура файлов

```
├── orders_restore.php    # Основной скрипт восстановления
├── orders.csv           # Список ID заказов для восстановления
├── restore.log          # Лог выполнения операций
├── cache.log            # Файл кэша (последний обработанный заказ)
└── vendor/              # Зависимости Composer
```

## 📊 Логирование

Все операции записываются в `restore.log` с временными метками:
```
[2025-11-06 13:49:51] Начало восстановления заказов
[2025-11-06 13:49:51] Начало обработки заказа: 212
[2025-11-06 13:49:52] Заказ восстановлен: #1234 -> ID: 567, Номер: #5678
```

## ⚠️ Важные примечания

- Автоматически пересчитывает общую скидку на основе скидок товаров
- При ошибке создания заказа скрипт останавливается для ручной проверки
- Используйте `cache.log` для возобновления прерванного процесса
- Скрипт автоматически пропускает уже обработанные заказы при перезапуске

**Примечание:** Всегда создавайте резервную копию данных перед использованием скрипта!
